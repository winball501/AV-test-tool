<Window x:Class="winball501_s_antivirus_test_tool.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:winball501_s_antivirus_test_tool"
        mc:Ignorable="d"
        Title="Settings" Height="450" Width="1002"
        Background="#2C2F37" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="10,20,10,10"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#388E3C"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Output Settings" FontWeight="Bold" FontSize="20"
                   Margin="0,0,0,15" Grid.Row="0"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <RadioButton Name="noLogRadio" Content="Don't log anything" IsChecked="True"/>
            <RadioButton Name="executedOnlyRadio" Content="Log only executed files"/>
            <RadioButton Name="notExecutedOnlyRadio" Content="Log only not executed files"/>
            <RadioButton Name="allFilesRadio" Content="Log executed and not executed files"/>
        </StackPanel>

        <CheckBox Name="saveStatusCheckBox" 
                  Content="When the program is closed and opened, continue the when pressed start execute from where you left off for same folder" 
                  Grid.Row="2"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Name="saveButton" Click="SaveButton_Click"/>
            <Button Content="Close" Name="cancelButton" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
